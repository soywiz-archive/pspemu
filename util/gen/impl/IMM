@ADDI   
	sRT = (RS + IMM );
	
@ADDIU
	sRT = (RS + IMM );

@SLTI
	sRT = (cast(int) RSU < IMM );
	
@SLTIU
	sRT = (cast(uint)RSU < IMMU);

@ANDI
	sRT = (RS & IMMU);

@ORI
	sRT = (RS | IMMU);

@XORI
	sRT = (RS ^ IMMU);

@LUI
	sRT = (IMM << 16);